
<script type="text/javascript">
  const token = '<%= @token %>';
  const name = '<%= @name %>';
  const moderator_env_name = '<%= @moderator_name %>';
  (function() {
    if(window.localStorage) {
      if(!localStorage.getItem('screenshareFirstLoad')) {
        localStorage['screenshareFirstLoad'] = true;
        window.location.reload();
      }
    } else {
      localStorage.removeItem('screenshareFirstLoad');
    }
  })();
</script>

<header>
  <%= render partial: 'header' %>
</header>

<main class='app'>
  <div class="videos">
    <div class="screenshare" id="screenshare"></div>
    <div class="audio" id=""></div>
  </div>

  <aside class="chat">
    <%= render partial: 'chat' %>
  </aside>

  <%= render partial: 'button-chat' %>
</main>
